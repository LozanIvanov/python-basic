# 🐍 Python Basics

A collection of beginner-friendly Python exercises covering loops, lists, arrays, functions, and working with user input.  
Each folder focuses on a specific programming concept.

---

## 📂 Structure

### 🌀 **01_loops/** → Loops and patterns  
- **triangle_numbers.py** → Prints numbers in an increasing triangle pattern  
- **reverse_stars.py** → Prints stars in decreasing order  

---

### 🧮 **02_lists/** → Lists and averages  
- **average_of_list.py** → Calculates the average of numbers in a list  
- **sum_positive_numbers.py** → Finds the sum of positive numbers  

---

### 🔢 **03_input_max/** → User input and finding the largest number  
- **find_largest_number.py** → User enters 5 numbers, program prints the largest  
- **find_youngest_person.py** → Finds the youngest person from user input  

---

### 🧩 **04_arrays/** → Arrays (merge, search, sorting, sums)  
- **double_elements.py** → Creates a new array with elements multiplied by 2  
- **merge_two_arrays.py** → Combines two arrays entered by the user  
- **merge_sorted_arrays.py** → Merges two already sorted arrays into one sorted array  
- **search_number_in_array.py** → Counts how many times a number appears  
- **check_sorted_array.py** → Checks if an array is sorted in ascending order  
- **check_element_exists.py** → Checks if an element exists in the array  
- **delete_element_by_index.py** → Deletes an element at a given position  

---

### 👥 **05_people/** → Working with names and ages  
- **youngest_person.py** → Finds the youngest person by comparing ages  
- **people_search.py** → Allows searching for a name inside a list of people (optional future task)

---

### ⚙️ **06_functions/** → Functions and reusable code  
- **say_hello_function.py** → Takes a name and prints “Hello, name”  
- **sum_two_numbers.py** → Takes two double numbers and returns their sum  
- **print_numbers_to_n.py** → Prints numbers from 1 to N  

---

## 🚀 **How to run**
Open a terminal in the project folder and run any file with:

```bash
python folder_name/file_name.py
